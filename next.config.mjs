/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment for static export (deploy as plain HTML to GitHub Pages, Netlify, etc.):
  // output: 'export',
  // images: { unoptimized: true }
};

export default nextConfig;
